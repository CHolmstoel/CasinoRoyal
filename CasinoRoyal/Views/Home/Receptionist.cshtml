@*@model KitchenStaffViewModel

@{
    ViewData["Title"] = "Hotel Rooms";
}

<h1>Hotel Rooms</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.HotelRoomID)
        </th>
    </tr>
    </thead>
    <tbody>
    @foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.HotelRoomID)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.HotelRoomID">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.HotelRoomID">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.HotelRoomID">Delete</a>
            </td>
        </tr>
    }
    </tbody>
</table>*@